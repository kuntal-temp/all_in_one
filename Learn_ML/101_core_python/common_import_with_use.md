# os
```
import os

os.makedirs('path/dir')
os.mkdir('path/dir')
os.chdir('../')
os.listdir('path') 
os.rmdir('path/dir') # can only remove empty directories to overcome this use [import shutil]

os.remove('path/file.txt')
os.rename('path/file.txt', 'path/New.txt')

os.path.getsize('path/file.txt')
os.path.exists('path/file.txt')
```


